services:
  - type: web
    name: survivor-os-2035
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
    plan: free
